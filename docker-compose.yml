version: '3'

services:

  nodejs:
    build: .
    image: yuttasakcom/nodejs-express:1.0.0
    container_name: nodejs
    environment:
      - HOST=nodejs
    ports:
      - "3000:3000"
    restart: always